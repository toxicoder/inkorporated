# Git Log Workflow

This workflow shows pretty logs.

## Parameters
- format: oneline, graph (default: graph).

## Steps
1. Validate inputs: Log to workflow_log.txt.
2. Run: git log --{format}.
3. Output: To console or file.
