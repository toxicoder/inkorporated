###############################################################################
# Bazel workspace for the Inkorporated project.
#
# This workspace defines the external dependencies required for building and
# testing the Inkorporated hybrid cloud infrastructure project.
#
# NOTE: External dependencies are now managed via MODULE.bazel using Bzlmod.
# This WORKSPACE file only contains workspace-level configuration.
#
# References:
#   https://bazel.build/external/module-setup
###############################################################################

workspace(name = "inkorporated")